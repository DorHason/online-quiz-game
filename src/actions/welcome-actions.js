export const changeCategoryAction = (dispatch, value) => {
  dispatch({
    type: "CHANGE_CATEGORY",
    value: value,
  });
};

export const changeDifficultyAction = (dispatch, value) => {
  dispatch({
    type: "CHANGE_CATEGORY",
    value: value,
  });
};

export const changeTypeAction = (dispatch, value) => {
  dispatch({
    type: "CHANGE_TYPE",
    value: value,
  });
};

export const changeAmountAction = (dispatch, value) => {
  dispatch({
    type: "CHANGE_AMOUNT",
    value: value,
  });
};
